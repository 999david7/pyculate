due to me not being able to upload the working .exe file please use a tool called pyinstaller to convert the source code into a .exe file
if you open the ide something like pycharm download the pyinstaller plugin using pip install pyinstaller
then use python -m PyInstaller main.py to actually turn the code into a exe
